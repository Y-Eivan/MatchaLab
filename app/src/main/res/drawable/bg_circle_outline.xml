<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval">

    <!-- The Fill Color (Equivalent to your @color/surface) -->
    <solid android:color="@color/surface" />

    <stroke
        android:width="6dp"
        android:color="@color/accent" />
</shape>